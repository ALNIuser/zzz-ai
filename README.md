## Запуск

### 1) На AI-сервере (Ubuntu 192.168.1.2)
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt

cp .env.example .env
nano .env



/etc/asterisk/extensions_custom.conf
; =========================================================
; AI ИНЖЕНЕР СКС СЕРВИС - ДИАПЛАН С ОТЛАДКОЙ
; =========================================================

[ai-entry]
exten => s,1,NoOp(=== AI SUPPORT ENTRY ===)
 same => n,Stasis(ai_support_ari)
 same => n,Hangup()

[from-internal-custom]
exten => 9999,1,NoOp(Call to AI Support)
 same => n,Dial(Local/s@ai-entry)
 same => n,Hangup()

 /etc/asterisk/ari.conf
[general]
enabled = yes
pretty = yes
allowed_origins = *

[ai_ari_user]
type = user
read_only = no
password = 1
/etc/asterisk/http.conf
#include http_additional.conf
#include http_custom.conf
[general]
enabled = yes
bindaddr = 0.0.0.0
bindport = 8088

/etc/asterisk/pjsip*.conf
#include pjsip.aor_custom.conf

[101]
type=aor
max_contacts=1
remove_existing=yes
maximum_expiration=7200
minimum_expiration=60
qualify_frequency=60

[102]
type=aor
max_contacts=1
remove_existing=yes
maximum_expiration=7200
minimum_expiration=60
qualify_frequency=60

[103]
type=aor
max_contacts=1
remove_existing=yes
maximum_expiration=7200
minimum_expiration=60
qualify_frequency=60

;--------------------------------------------------------------------------------;
;          Do NOT edit this file as it is auto-generated by FreePBX.             ;
;--------------------------------------------------------------------------------;
; For information on adding additional paramaters to this file, please visit the ;
; FreePBX.org wiki page, or ask on IRC. This file was created by the new FreePBX ;
; BMO - Big Module Object. Any similarity in naming with BMO from Adventure Time ;
; is totally deliberate.                                                         ;
;--------------------------------------------------------------------------------;
#include pjsip.auth_custom.conf

[101-auth]
type=auth
auth_type=userpass
password=1358
username=101

[102-auth]
type=auth
auth_type=userpass
password=1
username=102

[103-auth]
type=auth
auth_type=userpass
password=1
username=103
